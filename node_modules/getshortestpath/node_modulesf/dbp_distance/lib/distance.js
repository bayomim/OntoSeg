/**
 * @author: Mostafa Bayomi
 * bayomim@tcd.ie
 */
var fs = require('fs');
var _ = require('underscore');
var ObjAr;
(function () { // initialisation
    var array = fs.readFileSync('dbp_on_2014-.txt');
    ObjAr = JSON.parse(array);
    console.log("File is loaded");
})();

exports.distance = function (a, b) {
    if (a == b) {
       /* if (a == "owl#Thing") {
            
            return 99;
        }*/
       return 0;
    }
    else {
        var fin = _.find(ObjAr, function (obj) {
            return ((obj.first == a && obj.second == b) || (obj.first == b && obj.second == a));
        });
        console.log("DDDD %s:" + fin.distance)
        return fin.distance;
    }
}
